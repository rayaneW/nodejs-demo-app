name: Checkout Example

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  checkout-example:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify repository contents
        run: ls -alhR
